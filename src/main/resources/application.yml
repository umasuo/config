server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/umasuo/test-config-repo
          username: ourusername
          password: ourpassword
